# MEGSI-SETUP

## Find the VM IP address

```
ip a
```

## Connect to VM using ssh terminal

## Execute the command bellow on terminal

```
sudo rm -rf *.sh && curl -L -o menu.sh https://raw.githubusercontent.com/rafaelprudente/MEGSI-SETUP/main/UBUNTU/menu.sh && sudo chmod +x menu.sh && sudo ./menu.sh
```
